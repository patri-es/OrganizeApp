# Backend

Proyecto Backend movido al directorio raíz Backend.

Estructura simplificada: todo el código del proyecto está directamente en la carpeta Backend.

Mantén el archivo appsettings.json y launchSettings.json para la configuración local.
